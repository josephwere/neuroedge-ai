
version: '3.8'
services:
  frontend:
    build: .
    expose: ['3000']
    environment:
      - NODE_ENV=production
  caddy:
    image: caddy:2
    ports: ['443:443','80:80']
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile
    depends_on:
      - frontend
